# dependencies
node_modules/
node_modules
**/node_modules/
**/node_modules/**
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
# Local/secret overrides stay out; the shared non-secret defaults that builds
# depend on (API + site URLs) are committed so a fresh clone builds correctly.
.env*
!.env.example
!.env.development
!.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
