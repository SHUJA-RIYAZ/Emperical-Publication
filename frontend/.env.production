# Loaded by `next build` / `next start` — this is what gets baked into the
# bundle you zip up to cPanel.
NEXT_PUBLIC_API_URL=https://irmms.org/emperical-api/api
NEXT_PUBLIC_SITE_URL=https://irmms.org/emperical-publication
